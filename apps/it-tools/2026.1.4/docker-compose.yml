services:
  it-tools:
    image: sharevb/it-tools:2026.1.4
    container_name: ${CONTAINER_NAME}
    restart: always
    networks:
      - 1panel-network
    ports:
      - ${PANEL_APP_PORT_HTTP}:8080
    labels:
      createdBy: "Apps"
networks:
  1panel-network:
    external: true
